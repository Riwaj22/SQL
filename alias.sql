-- alias

-- alias is used to assign an alias to the column or a table

-- select column name as column alias
-- from table name

select Customer_id as "Serial Number", Customer_name as name , age as customer_age from customer