-- -- select_distinct

-- the distinct keyword is used in conjuction with the select statement to eliminate all the duplicate records and fetching only unique records

select distinct first_name,age from customer_table
select distinct * from customer_table

